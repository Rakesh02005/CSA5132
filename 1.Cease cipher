#include<stdio.h>
#include<string.h>

int main(){
    char t[100]; int k,i;
    printf("Text: "); scanf("%s",t);
    printf("Key: "); scanf("%d",&k);
    for(i=0;t[i];i++) t[i]=((t[i]-65+k)%26)+65;
    printf("Cipher: %s",t);
}

output:
Text: HELLO
Key: 3
Cipher: KHOOR

